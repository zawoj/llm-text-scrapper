seeds:
  - https://docs.example.com

output_dir: corpus
max_concurrency: 5
restart_browser_every: 50
rate_limit_seconds: 1.0

ignore_paths:
  - /login
  - /search
  - /blog
  - /api

max_pages: 5000
page_timeout: 30000
max_retries: 3
log_level: INFO
state_dir: _meta
